import React from 'react'
import FormComponent from '../form-contact/FormContactUs'
interface Props {
    img: string
}
export const ContactInPrograms = ({ img }: Props) => {
    return (
        <div>
        </div>
    )
}
